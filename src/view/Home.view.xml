<mvc:View
	controllerName="com.ts.tsReport.controller.Home"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">
    <!-- Page -->
    <MessagePage
        title="{i18n>app_title}"
        text="{i18n>app_title}"
        icon="sap-icon://accept"
        id="page"
        description="{i18n>app_description}">
        <!-- Buttons Action -->
        <buttons>
            <Button
                text="{i18n>btn_text}"
                press="onOpenDialog" />
        </buttons>
    </MessagePage>
</mvc:View>